# Additional dependencies for hybrid search implementation
# Add these to your existing requirements.txt

# Core dependencies (if not already included)
numpy>=1.24.0
scipy>=1.10.0

# Optional: For enhanced text processing
nltk>=3.8.0
scikit-learn>=1.3.0  # For additional similarity metrics

# For testing
pytest>=7.0.0
pytest-asyncio>=0.21.0